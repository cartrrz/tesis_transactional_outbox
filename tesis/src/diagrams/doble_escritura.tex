\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=1.35cm and 2.0cm,
        every node/.style={font=\small}
    ]
        \node[tesisService, minimum width=3.5cm] (service) {Microservicio\\de negocio};
        \node[tesisDatabase, below left=1.2cm and 2.0cm of service, minimum width=2.8cm] (db) {Base de\\datos};
        \node[tesisBroker, below right=1.2cm and 2.0cm of service, minimum width=3.1cm] (broker) {Broker de\\eventos};

        \node[tesisProcess, below=of db, minimum width=3.0cm] (ok) {Operación\\confirmada};
        \node[tesisWarning, below=of broker, minimum width=3.0cm] (fail) {Publicación\\fallida};
        \node[tesisWarning, below right=1.15cm and -2.2cm of ok, minimum width=7.5cm] (result) {Resultado: inconsistencia entre base de datos y mensajería};

        \draw[tesisArrow] (service) -- node[above left, font=\scriptsize] {1. guardar datos} (db);
        \draw[tesisDashedArrow] (service) -- node[above right, font=\scriptsize] {2. publicar evento} (broker);
        \draw[tesisArrow] (db) -- (ok);
        \draw[tesisDashedArrow] (broker) -- (fail);
        \draw[tesisDashedArrow] (ok) -- (result);
        \draw[tesisDashedArrow] (fail) -- (result);
    \end{tikzpicture}

    \caption{Escenario de inconsistencia producido por el problema de la doble escritura.}
    \label{fig:doble_escritura}
\end{figure}
