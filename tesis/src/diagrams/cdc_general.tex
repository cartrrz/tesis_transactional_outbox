\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=1.05cm,
        every node/.style={font=\small}
    ]
        \node[tesisService, minimum width=3.8cm] (app) {Aplicación Spring Boot};
        \node[tesisDatabase, below=of app, minimum width=3.4cm] (db) {Base de datos};
        \node[tesisProcess, below=of db, minimum width=3.8cm] (cdc) {Motor CDC\\Debezium};
        \node[tesisBroker, below=of cdc, minimum width=4.0cm] (broker) {Broker de eventos\\Kafka};

        \draw[tesisArrow] (app) -- node[right, font=\scriptsize] {escritura} (db);
        \draw[tesisArrow] (db) -- node[right, font=\scriptsize] {transaction log} (cdc);
        \draw[tesisArrow] (cdc) -- node[right, font=\scriptsize] {eventos} (broker);
    \end{tikzpicture}

    \caption{Arquitectura general de Change Data Capture.}
    \label{fig:cdc_general}
\end{figure}
