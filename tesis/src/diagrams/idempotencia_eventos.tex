\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=1.0cm and 1.25cm,
        every node/.style={font=\small}
    ]
        \node[tesisBroker, minimum width=3.3cm] (broker) at (0,1.8) {Broker de eventos};
        \node[tesisService, minimum width=3.6cm] (consumer) at (0,0.45) {Consumidor idempotente};
        \node[tesisProcess, minimum width=3.5cm] (check) at (0,-0.95) {Verificar ID del evento};

        \node[tesisDatabase, minimum width=2.9cm] (processed) at (-4.0,-0.95) {Eventos\\procesados};
        \node[tesisProcess, minimum width=3.1cm] (business) at (4.0,-0.95) {Lógica de\\negocio};
        \node[tesisWarning, minimum width=3.1cm] (discard) at (0,-2.45) {Duplicado:\\descartar};

        \draw[tesisArrow] (broker) -- (consumer);
        \draw[tesisArrow] (consumer) -- (check);
        \draw[tesisArrow] (check) -- (processed);
        \draw[tesisArrow] (check) -- (business);
        \draw[tesisDashedArrow] (processed.south east) -- (discard.west);
    \end{tikzpicture}

    \caption{Control de eventos duplicados mediante consumidor idempotente.}
    \label{fig:idempotencia_eventos}
\end{figure}
