\begin{figure}[H]
    \centering

    \begin{tikzpicture}[
        font=\small,
        box/.style={
            draw,
            rounded corners,
            minimum width=3cm,
            minimum height=0.9cm,
            align=center,
            fill=gray!10
        },
        store/.style={
            draw,
            cylinder,
            shape border rotate=90,
            aspect=0.25,
            minimum width=2.6cm,
            minimum height=1cm,
            align=center,
            fill=gray!20
        },
        arrow/.style={
            -Latex,
            thick
        },
        dashedarrow/.style={
            -Latex,
            thick,
            dashed
        }
    ]

\node[box] (broker) at (0,3) {Broker de Eventos};

\node[box] (consumer) at (0,1) {Consumidor Idempotente};

\node[store] (processed) at (-3.5,-1.8) {Eventos\\Procesados};

\node[box] (business) at (3.5,-1.8) {Lógica de\\Negocio};

\draw[arrow] (broker) -- node[right]{\scriptsize Evento} (consumer);

\draw[arrow] (consumer) -- node[above left]{\scriptsize 1. Verificar ID} (processed);

\draw[arrow] (consumer) -- node[above right]{\scriptsize 2. Procesar} (business);

\draw[dashedarrow] (processed.south) -- ++(0,-0.6) -| node[pos=0.25, below]{\scriptsize Si ya existe: descartar} (consumer.south);

    \end{tikzpicture}

    \caption{Control de eventos duplicados mediante consumidor idempotente.}
    \label{fig:idempotencia_eventos}

\end{figure}