\begin{figure}[H]
    \centering

    \begin{tikzpicture}[
        font=\small,
        service/.style={
            draw,
            rounded corners,
            minimum width=2.8cm,
            minimum height=1cm,
            align=center,
            fill=gray!10
        },
        brokerbox/.style={
            draw,
            rounded corners,
            minimum width=4cm,
            minimum height=1.2cm,
            align=center,
            fill=gray!20
        },
        arrow/.style={
            -Latex,
            thick
        }
    ]

\node[service] (producer1) at (-4,2) {Servicio\\Pedidos};
\node[service] (producer2) at (4,2) {Servicio\\Usuarios};

\node[brokerbox] (broker) at (0,0) {Broker de Eventos\\Kafka / RabbitMQ};

\node[service] (consumer1) at (-4,-2) {Inventario};
\node[service] (consumer2) at (0,-2) {Pagos};
\node[service] (consumer3) at (4,-2) {Notificaciones};

\draw[arrow] (producer1) -- (broker);
\draw[arrow] (producer2) -- (broker);

\draw[arrow] (broker) -- (consumer1);
\draw[arrow] (broker) -- (consumer2);
\draw[arrow] (broker) -- (consumer3);

    \end{tikzpicture}

    \caption{Broker de eventos como intermediario entre productores y consumidores.}
    \label{fig:broker_eventos}

\end{figure}